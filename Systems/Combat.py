def start_combat_handler(msg, tick):
    #Double check if char is in Wilderness
    #Get appropriate monster, basic attacks only
    #simulate combat every tick, one attack per tick
    # Start combat handler
    # Fight handler
    # End combat handler
    player = msg['Character']
    if player['current_area'] == 2:
        
        pass
    pass